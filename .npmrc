# Angular ships minor-version-locked packages (@angular/ssr, build-angular)
# whose peer ranges occasionally drift apart. legacy-peer-deps makes both local
# and Vercel installs resolve the same way the project was built with.
legacy-peer-deps=true
