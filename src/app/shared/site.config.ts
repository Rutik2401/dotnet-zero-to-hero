/** Production origin — used for canonical URLs, Open Graph, and sitemap.xml. */
export const SITE_URL = 'https://dotnet-zero-to-heroo.vercel.app';

/** Default social-share image (absolute URL). Place the file in /public. */
export const DEFAULT_OG_IMAGE = `${SITE_URL}/og-default.png`;

/** Brand name appended where useful. */
export const SITE_NAME = 'Learn Hub';
